export function ComboHero() {
  return (
    <header className="border-b border-[var(--border-subtle)] pb-8">
      <p className="text-metadata font-medium uppercase tracking-label text-brand">
        Evidence Combo Studio
      </p>
      <h1 className="mt-3 font-display text-3xl font-semibold tracking-display text-foreground md:text-4xl">
        Build an evidence-supported football combination
      </h1>
      <p className="mt-3 max-w-2xl text-sm leading-relaxed text-[var(--ink-secondary)] md:text-base">
        Choose a target odds range, risk profile, and supported markets. RankWagers
        assembles qualified selections with transparent evidence — then shows operators
        that may accept the combination. Odds may change. No guaranteed returns.
      </p>
    </header>
  );
}
